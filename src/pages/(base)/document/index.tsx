/**
 * @handle {
 *  "icon": "solar:document-linear",
 *  "order": 3
 * }
 */

import { replace } from 'react-router-dom';
const Document = () => {
  return null;
};

export const loader = () => {
  replace('router');
};

export default Document;
