/**
 * @handle {
 *   "icon": "lucide:menu",
 *   "order": 999
 * }
 */

import { replace } from 'react-router-dom';

const MultiMenu = () => {
  return null;
};

export const loader = () => {
  return replace('first');
};

export default MultiMenu;